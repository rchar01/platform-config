---
- name: Exercise host-local request role integration
  hosts: localhost
  connection: local
  become: true
  gather_facts: false
  tasks:
    - name: Run the structurally fixed request action
      ansible.builtin.include_role:
        name: pki_host_local_certificate
        tasks_from: request
