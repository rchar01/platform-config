---
- name: Exercise host-local trust bootstrap role integration
  hosts: localhost
  connection: local
  become: true
  gather_facts: false
  tasks:
    - name: Run the structurally fixed trust bootstrap action
      ansible.builtin.include_role:
        name: pki_host_local_certificate
        tasks_from: trust
