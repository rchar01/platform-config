---
- name: Validate mocked external probe inputs before lifecycle mutation
  ansible.builtin.assert:
    that:
      - >-
        inventory_hostname
        != (openbao_observers_test_invalid_probe_host | default(''))
    fail_msg: "Mocked external probe inputs are invalid."
