---
- name: Require mocked OpenBao service order
  ansible.builtin.assert:
    that:
      - openbao_test_role_order == ['firewalld', 'podman_host']

- name: Record mocked OpenBao service
  ansible.builtin.set_fact:
    openbao_test_role_order: "{{ openbao_test_role_order + ['openbao'] }}"
