---
- name: Recover one interrupted host-local registry certificate lifecycle
  hosts: registry
  become: true
  gather_facts: false
  tasks:
    - name: Run structurally fixed registry certificate recovery boundary
      ansible.builtin.include_role:
        name: pki_host_local_certificate
        tasks_from: recover
