---
- name: Revalidate one exported host-local registry deployment decision
  hosts: registry
  become: true
  gather_facts: false
  tasks:
    - name: Run structurally fixed registry certificate decision preflight boundary
      ansible.builtin.include_role:
        name: pki_host_local_certificate
        tasks_from: decision_preflight
