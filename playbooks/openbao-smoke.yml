---
- name: Check active OpenBao and HAProxy state
  hosts: openbao
  become: true
  gather_facts: true
  strategy: linear
  any_errors_fatal: true
  tasks:
    - name: Run shared direct OpenBao and all-HAProxy smoke
      ansible.builtin.import_tasks: tasks/openbao-smoke.yml
