---
- name: Read authenticated host-local registry certificate status
  hosts: registry
  become: true
  gather_facts: false
  tasks:
    - name: Run structurally fixed registry certificate status boundary
      ansible.builtin.include_role:
        name: pki_host_local_certificate
        tasks_from: status
