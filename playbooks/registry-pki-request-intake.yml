---
- name: Intake one exact directly retrieved registry certificate request
  hosts: registry
  become: true
  gather_facts: false
  tasks:
    - name: Run structurally fixed direct request intake boundary
      ansible.builtin.include_role:
        name: pki_host_local_certificate
        tasks_from: request_intake
