---
- name: Export one authenticated host-local registry deployment evidence attempt
  hosts: registry
  become: true
  gather_facts: false
  tasks:
    - name: Run structurally fixed registry certificate evidence export boundary
      ansible.builtin.include_role:
        name: pki_host_local_certificate
        tasks_from: evidence_export
