---
- name: Activate one authenticated host-local registry certificate response
  hosts: registry
  become: true
  gather_facts: false
  tasks:
    - name: Run structurally fixed registry certificate activation boundary
      ansible.builtin.include_role:
        name: pki_host_local_certificate
        tasks_from: activate
