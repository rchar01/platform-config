---
- name: Intake one exact directly retrieved registry deployment evidence attempt
  hosts: registry
  become: true
  gather_facts: false
  tasks:
    - name: Run structurally fixed direct evidence intake boundary
      ansible.builtin.include_role:
        name: pki_host_local_certificate
        tasks_from: evidence_intake
