---
- name: Reload systemd
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Restart node_exporter
  ansible.builtin.systemd_service:
    name: "{{ node_exporter_service_name }}"
    state: restarted
  when:
    - node_exporter_service_enabled | bool
    - not ansible_check_mode
