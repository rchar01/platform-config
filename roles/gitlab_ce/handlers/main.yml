---
- name: Reload systemd
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Restart GitLab CE
  ansible.builtin.systemd_service:
    name: "{{ gitlab_ce_service_name }}"
    state: restarted
  when:
    - gitlab_ce_service_enabled | bool
    - not ansible_check_mode
