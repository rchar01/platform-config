---
- name: Restart HAProxy workload LB
  ansible.builtin.systemd_service:
    name: "{{ haproxy_workload_lb_service_name }}"
    state: restarted
  when:
    - haproxy_workload_lb_service_enabled | bool
    - not ansible_check_mode
