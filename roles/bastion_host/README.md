# bastion_host

Prepares a generic platform bastion host by installing basic tools and creating the `/opt/platform` directory layout.

Default layout:

```text
/opt/platform/
+-- bin/
+-- etc/
+-- lib/
+-- runtime/
```
