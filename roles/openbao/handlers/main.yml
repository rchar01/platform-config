---
- name: Restart OpenBao HA service
  ansible.builtin.systemd_service:
    name: "{{ openbao_service_name }}"
    state: restarted
  when:
    - openbao_service_enabled | bool
    - openbao_service_state == 'started'
    - not ansible_check_mode
