---
- name: Verify firewall immediately before OpenBao HAProxy reload
  ansible.builtin.include_tasks: firewall_guard.yml

- name: Reload verified OpenBao HAProxy service
  ansible.builtin.systemd_service:
    name: "{{ openbao_haproxy_service_name }}"
    state: reloaded
