# group_vars examples

Use the `.example` files under `inventories/*/group_vars/` as the source shape for private group variables.

Real group variables belong in `platform-private/config/inventories/<environment>/group_vars/`.
