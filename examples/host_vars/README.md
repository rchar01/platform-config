# host_vars examples

Use the `.example` files under `inventories/*/host_vars/` as the source shape for private host variables.

Real host variables belong in `platform-private/config/inventories/<environment>/host_vars/`.
