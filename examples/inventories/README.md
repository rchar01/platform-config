# Inventory examples

The primary public examples live under `inventories/*/*.example` so that real inventory files are not committed accidentally.

Copy those examples into `platform-private/config/inventories/` and replace placeholders with real values there.
