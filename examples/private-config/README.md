# Private Config Examples

These files are safe examples for `../platform-private/config/*.ansible.env`.

Copy them into `platform-private/config/`, remove the `.example` suffix, and adjust paths for your workspace. They should contain paths and environment selection only, not secret values.
